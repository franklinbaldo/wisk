CREATE TABLE "RunCheck" (
    "id" VARCHAR,
    "run" VARCHAR,
    "kind" VARCHAR,
    "procedure" VARCHAR,
    "result" VARCHAR,
    "status" VARCHAR,
    "evidence" VARCHAR,
    "goal" VARCHAR,
    "observed_at" TIMESTAMPTZ
);
