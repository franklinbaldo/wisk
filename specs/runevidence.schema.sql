CREATE TABLE "RunEvidence" (
    "id" VARCHAR,
    "run" VARCHAR,
    "kind" VARCHAR,
    "reference" VARCHAR,
    "summary" VARCHAR,
    "goal" VARCHAR,
    "decision" VARCHAR,
    "observed_at" TIMESTAMPTZ
);
