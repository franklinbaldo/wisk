CREATE TABLE "RunOutcome" (
    "id" VARCHAR,
    "run" VARCHAR,
    "result_state" VARCHAR,
    "work_status" VARCHAR,
    "summary" VARCHAR,
    "next_move" VARCHAR,
    "evidence" VARCHAR[],
    "checks" VARCHAR[]
);
